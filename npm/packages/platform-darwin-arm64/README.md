# @keydris/cli-darwin-arm64

Native `keydris` CLI binary for **darwin arm64**.

This is a platform-specific dependency of [`@keydris/cli`](https://www.npmjs.com/package/@keydris/cli). Install `@keydris/cli` instead — npm selects and installs the correct platform package automatically. Do not depend on this package directly.
